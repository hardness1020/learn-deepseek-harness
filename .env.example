# Live demos only (sections 04+). Copy to .env and fill in. Offline checks need none of this.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Optional: override the API endpoint (proxies, gateways).
# ANTHROPIC_BASE_URL=https://api.anthropic.com
