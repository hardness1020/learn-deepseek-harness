# 00 · Setup

> **Stub.** This section's README is drafted after the spec locks. The `src/`
> here exists so section 01's carry-forward is concrete: the Message shape and
> the Scripted stand-in that every later Offline check reuses.

```bash
python sections/00-setup/src/test.py   # offline checks, no key
```
